# 更新日志（CHANGELOG）

## v1.2.1（2026-08-12）· 消息筛选 / 私信优化 / 违禁词管理 / 自动禁言

> 本版本为体验与治理增强：消息中心分类筛选、私信界面优化、后台违禁词库管理与违规用户名单、3 天 5 次违规自动禁言。

### ✨ 新增功能

- **消息中心筛选 Tab**：全部 / 点赞 / 评论回复 / 私信 / 广播 分类查看。
- **私信界面优化**：对话气泡显示双方头像；时间显示友好化（今天 / 昨天 / 月日）；**Ctrl+Enter 快捷发送**；发送后自动滚动到底并保持输入焦点。
- **后台违禁词库**：仪表盘可视化查看 / 添加 / 删除违禁词（去重、≤20 字，**即时生效**，无需改代码）；词库存于数据库并随数据文件持久化。
- **违规用户名单**：后台仪表盘展示违规用户聚合信息（违规次数、来源分布、最近时间、当前禁言状态）。
- **自动禁言（无审核员治理升级）**：同一用户 **3 天内发布违规内容（敏感词拦截）累计 5 次 → 自动禁言 3 天**，并通过**系统消息**通知本人；管理员豁免；不缩短已有更长禁言。
- **系统消息类型**：消息中心新增 `system` 类型（自动禁言等系统通知）。

### 🐛 修复

- **注销不清理违规记录（数据一致性）**：修复前注销账号会残留违规记录；现级联清理（违规名单接口也会过滤已删用户）。
- **违禁词库未导出增删函数（代码错误）**：`lib/sensitive.js` 补充导出 `addWord / removeWord`，后台增删接口恢复可用。

### 🔒 安全加固

- 违禁词列表展示经 HTML 转义（防 XSS）。
- 词库接口全部要求管理员鉴权（普通用户访问 403）。

### 📝 文档

- 更新 `README.md`（版本号、功能总览、API 速览、安全说明）。
- 版本号：`1.2.1`。

## v1.2.0（2026-08-12）· 消息 / 私信 / 拉黑 / 热度排序 / 搜索双 Tab

> 本版本为社区互动大版本：新增消息通知体系、私信、拉黑、评论回复、全站广播、热度排序、搜索「日记 / 用户」双 Tab，并落地「无审核员内容审核方案」第一道防线（敏感词过滤）。

### ✨ 新增功能

- **消息中心**：导航栏铃铛入口 + 未读红点（每分钟刷新）；点赞 / 评论 / 回复 / 广播 / 私信均推送消息；支持全部已读。
- **评论回复**：支持楼中楼嵌套（回复 @某人），评论被回复自动通知；删除评论时其回复一并清理。
- **私信**：他人主页「私信」按钮进入对话视图，双方消息按会话线程组织，进入对话自动已读；禁言 / 被拉黑期间不可发送。
- **拉黑**：他人主页「拉黑 / 解除」；被拉黑后无法私信、评论、点赞、收藏、转发对方内容（互动全隔离，参照 B 站）。
- **全站广播**：管理员仪表盘新增广播表单，所有用户在消息中心收到广播通知。
- **广场热度排序**：点赞量优先 + 发布时间近加权（72 小时内新日记加分），卡片显示 🔥 热度徽标。
- **搜索升级**：搜索框移至导航栏「写日记」旁（全局可用）；搜索结果顶部「日记 / 用户」双 Tab——日记按内容，用户按用户名 / 昵称。
- **用户搜索**：`/api/users/search`，搜索结果含公开日记数 / 获赞数。
- **无审核员内容审核方案**：敏感词过滤落地（发布 / 编辑日记、评论、私信、广播全拦截，含全角 / 大小写 / 分隔符变体对抗）；完整方案文档见 `docs/无审核员内容审核方案.pdf`。

### 🐛 修复

- **用户注销不清理消息（数据一致性）**：修复前注销账号会残留指向已删用户的站内消息（消息中心出现"未知用户"）；现级联清理其发出与收到的消息（全站广播保留）。

### 🔒 安全加固

- 拉黑后**点赞 / 收藏 / 转发**同样被拦截（此前仅评论 / 私信受限）。
- 评论回复目标校验：parentId 必须属于同一篇日记，防止跨日记伪造回复。
- 私信发送方 / 接收方由服务端指定，客户端不可伪造；敏感词命中即拦截。

### 📝 文档

- 更新 `README.md`（功能总览、目录结构、API 速览、安全说明、版本号）。
- 新增《无审核员内容审核方案》（`docs/`，HTML + PDF）。
- 版本号：`1.2.0`。

## v1.1.0（2026-08-12）· 评论 / 他人主页 / 后台管理增强

> 本版本为功能大版本：新增评论区、他人主页、后台改密、用户角色管理与处罚系统，并修复地图与若干安全问题。

### ✨ 新增功能

- **评论区**：日记详情页新增评论区——发表评论**无需审核**即时显示；删除权限为评论者本人 / 日记作者 / 管理员三者之一；评论内容经 HTML 转义防 XSS；评论与日记、用户的级联删除完整。
- **他人主页**：点击任意头像 / 昵称进入对方主页（`/user.html?id=`），展示公开资料（昵称 / 头像 / 签名 / 居住地 / 星座 / 注册时间）与 TA 的公开日记；**不包含**个人编辑、修改密码等区域；生日只显示星座、不暴露完整日期（隐私保护）。
- **后台仪表盘修改密码**：管理员后台首页新增修改密码表单，改后所有会话失效需重新登录。
- **用户角色管理**：后台用户管理可将普通用户**设为管理员 / 降为普通用户**；新管理员会话立即生效；**最后一个管理员不可降级**。
- **处罚系统（禁言）**：后台可对普通用户执行**禁言 1 天 / 禁言 1 周 / 永久禁言 / 解除禁言**；禁言期间无法发布、编辑日记、上传配图与发表评论（可正常登录浏览）；管理员不可被处罚；后台统计新增「禁言中」卡片。
- **最近登录显示国家 / 省份**：个人中心不再显示 IP 数字，改为显示 IP 对应的**国家与省份**（通过免费 ipwho.is 接口异步查询，本机 / 内网显示「本机访问」，查询失败显示「未知」）。

### 🐛 修复

- **地图无法显示**：瓦片源由 OpenStreetMap 改为**高德地图**（国内访问稳定），并补充本地 Leaflet 托管（v1.0.03 已本地化地图库）。
- **禁言绕过漏洞（安全）**：修复前禁言用户仍可通过**编辑已有日记**或**上传配图**绕过禁言；现发布、编辑、上传、评论四条路径统一拦截。
- **评论级联删除缺失（数据一致性）**：修复前删除日记 / 用户时评论会残留为孤儿数据；现级联函数同步清理评论。
- **他人主页生日泄露（隐私）**：公开主页不再返回完整出生日期，仅保留星座。

### 📝 文档

- 更新 `README.md`（功能总览、目录结构、API 速览、地图与安全说明、版本号）。
- 新增本版本更新日志。
- 版本号：`1.1.0`。

## v1.0.03（2026-08-12）· 修复写日记/地图/IP/转发

> 本版本修复用户实测反馈的 4 个问题：发布按钮无反应、位置选择不可用、IP 显示异常、转发复制兼容。

### 🐛 关键修复

- **写日记发布按钮无反应（严重）**：根因是 `write.js` 中使用 `let location` 声明变量，与浏览器内置的 `window.location`（当前网址）冲突，触发 `SyntaxError: Identifier 'location' has already been declared`，**整个脚本不执行**，导致发布按钮未绑定事件、地图未初始化。现统一改为 `selectedLoc`，并改用 `window.location.href` 跳转。
- **地图选点不可用**：Leaflet 此前从 unpkg CDN 加载，国内网络经常加载失败导致地图空白。现**将 Leaflet 1.9.4 本地托管**（`public/vendor/leaflet/`，含图标资源），地图库不再依赖外网 CDN；瓦片仍使用 OpenStreetMap（需联网）。
- **IP 显示异常**：本地访问时显示 `::1`（IPv6 回环）令人困惑。现做归一化：IPv4-mapped IPv6（`::ffff:x.x.x.x`）转换为 IPv4、`::1` 归一为 `127.0.0.1`；前端将本机地址显示为「本机访问（127.0.0.1）」。

### ✨ 体验增强

- **地点名称可手动填写**：地图选点后自动识别地名（Nominatim 逆地理编码，3 秒超时防卡死）；识别失败时回退为坐标，且新增「地点名称」输入框，可直接填写如「杭州西湖」。
- **转发复制兼容**：转发时复制日记链接到剪贴板并弹出提示。修复非 https 环境（如局域网 IP 访问）下 `navigator.clipboard` 不可用导致复制静默失败的问题，自动回退 `execCommand` 方案；复制失败时提示手动复制链接。

### 📝 文档

- 更新 `README.md`（功能总览、目录结构、地图说明、版本号）。
- 新增本版本更新日志。
- 版本号：`1.0.03`。

## v1.0.02（2026-08-12）· 新增修改密码与限流提示优化

> 本版本新增**修改密码**功能（个人中心），并优化登录限流提示，让用户明确知道「还要等多久」。

### ✨ 新增功能

- **修改密码**：个人中心新增「修改密码」区块（当前密码 + 新密码 + 确认新密码）；
  - 需验证**当前密码**（防他人改密），新密码规则与注册一致（8-16 位字母数字且含字母与数字，**管理员豁免字符集限制**）；
  - 新密码不能与当前密码相同；
  - **修改成功后该账号所有会话立即失效**（含当前会话），需重新登录，防止旧会话被冒用；
  - 管理员同样适用（在个人中心操作），后台登录态同步失效。

### 💡 体验优化

- **限流提示带剩余时间**：触发频率限制（429）时，提示改为「操作过于频繁，请 X 分钟后再试」，并在响应头携带 `Retry-After`（秒），不再只说「请稍后再试」。

### 📝 文档

- 更新 `README.md`（功能总览、API 速览、安全说明、版本号）。
- 新增本版本更新日志。
- 版本号：`1.0.02`。

## v1.0.01（2026-08-12）· 安全修复与数据一致性

> 本版本为接手项目后的一轮**实测 + 修复**：通过自动化测试套件（71 项用例）全面验证前台 / 后台 / 安全边界，修复 1 个高危漏洞与多项中低危问题。

### 🐛 高危漏洞修复

- **上传存储型 XSS（高危）**：修复前攻击者可上传 `evil.html` 等脚本文件并伪装成图片（仅校验客户端声明的 MIME 类型），文件以 `text/html` 被托管后即可在站点域名下执行任意脚本（窃取登录态等）。现改为 **MIME 白名单 + 扩展名白名单双重校验**，落盘后再做**图片魔数校验**（校验真实文件头，不信任任何客户端声明），非真实图片一律拒绝并删除；头像与日记配图两条上传链路均已加固。
- **收藏列表越权可见**：作者将公开日记改为「仅自己可见 / 未通过」后，收藏过该日记的其他用户仍能在「我的收藏」看到内容。现收藏列表增加可见性过滤，与日记详情权限保持一致。

### 🔒 数据一致性修复（孤儿数据清理）

- 统一新增级联删除：**后台删除日记**、**后台删除用户**、**用户注销账号**三条路径均会同时清理该日记 / 用户关联的点赞、收藏、转发记录，杜绝「指向已删除日记 / 已删除用户」的孤儿数据（此前后台删除路径会残留）。

### 🛡️ 限流与防绕过加固

- **限流桶按接口拆分**：修复前注册 / 登录 / 后台登录共用一个计数桶（同一 IP 任意接口失败 10 次即全部锁定，后台管理员登录会被前台注册失败误伤）。现按 `scope`（register / login / admin-login）分桶，互不挤占；
- **只统计失败请求**：成功登录不再消耗限流额度，正常用户频繁登录不受影响；注册接口保留「成功也计数」以防范批量注册；
- **X-Forwarded-For 信任开关**：修复前无条件信任客户端可伪造的 `X-Forwarded-For` 头，攻击者可伪造 IP 绕过限流并伪造登录 IP。现默认忽略该头（`config.js` 的 `TRUST_PROXY: false`），仅部署在受信反向代理后手动开启。

### 🧹 体验与细节优化

- **头像旧文件清理**：更换头像时自动删除旧头像文件，避免磁盘垃圾累积；
- **默认头像健壮性**：昵称首字符为特殊字符（如 `<`、`&`）时不再破坏内嵌 SVG，自动回退为「友」；
- **资料编辑放宽**：居住地（国家/地区）不再强制必选（与后端一致），新用户首次编辑资料不再被卡。

### 📝 文档

- 更新 `README.md`（安全说明、部署说明、版本号）。
- 新增本版本更新日志。
- 版本号：`1.0.01`。

## v1.0.00（2026-08-12）· 全新版本

> 本版本为「拾光日记」升级改版，正式更名为 **「栖桉集」**，主色调由清新绿改为**淡蓝**（前台与后台全部界面）。

### ✨ 新增功能

- **地图发布地点**：发布 / 编辑日记时可点击地图选择发布地点（采用 **Leaflet + OpenStreetMap** 可商用开源标准地图，逆地理编码显示位置名称，失败时回退坐标）；日记详情页展示带标记的小地图，广场卡片与后台审核列表同步显示地点。
- **居住地选择**：编辑个人资料时，居住地（国家/地区）改为**下拉选择**，收录联合国全部 **193 个会员国 + 2 个观察员国/地区（梵蒂冈、巴勒斯坦）**，并注明「截止到上次更新时地球上只有这些国家和地区」；另保留「城市（选填）」输入框。
- **IP 显示**：个人主页显示最近登录 IP（注册 / 登录时自动记录）；获取不到时显示「未知」。
- **发布时间**：发布日记时自动记录发布时间，并在广场卡片、日记详情、我的日记、后台审核列表统一展示。
- **互动功能**：日记新增**点赞、转发、收藏**（广场 / 详情 / 我的日记 / 我的收藏均可操作）；转发会复制分享链接并计次；个人中心新增「我的收藏」列表。
- **注销账号**：普通用户与管理员均可注销本人账号（需输入密码二次确认，级联删除本人日记与互动数据）；出于安全考虑，**最后一个管理员不允许注销**。

### 🔒 安全加固（自查修补）

- **密码策略**：普通用户密码改为 **8-16 位、仅限字母和数字、须同时包含字母和数字**；管理员密码不受此限制；已创建账号密码不受影响。
- **密码存储**：所有密码均以「加盐 scrypt 散列」保存（`data/db.json` → `users[].passwordHash` + `users[].salt`），**绝不明文落盘**。
- **登录限流**：前台登录 / 注册、后台登录接口均加入 IP 频率限制（15 分钟内最多 10 次），防暴力破解与批量注册。
- **安全响应头**：新增 `X-Content-Type-Options`、`X-Frame-Options`、`Referrer-Policy` 与基础 CSP（限制脚本 / 图片 / 连接来源），并关闭 `X-Powered-By` 指纹。
- **输入校验**：地点经纬度范围校验、图片路径白名单（防路径穿越）、现有长度 / 格式校验保持。
- **统一错误文案**：登录失败统一返回「用户名或密码错误」，防止账号枚举。

### 🎨 界面改版

- 站点更名为 **栖桉集**，全站（前台 + 管理后台）主色调改为**淡蓝**。

### 📝 文档

- 更新 `README.md`（新功能、接口、部署说明）。
- 新增本更新日志 `CHANGELOG.md`。
- 版本号：`1.0.00`。
