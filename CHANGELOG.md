# 更新日志（CHANGELOG）

## v1.0.00（2026-08-12）· 全新版本

> 本版本为「拾光日记」升级改版，正式更名为 **「栖桉集」**，主色调由清新绿改为**淡蓝**（前台与后台全部界面）。

### ✨ 新增功能

- **地图发布地点**：发布 / 编辑日记时可点击地图选择发布地点（采用 **Leaflet + OpenStreetMap** 可商用开源标准地图，逆地理编码显示位置名称，失败时回退坐标）；日记详情页展示带标记的小地图，广场卡片与后台审核列表同步显示地点。
- **居住地选择**：编辑个人资料时，居住地（国家/地区）改为**下拉选择**，收录联合国全部 **193 个会员国 + 2 个观察员国/地区（梵蒂冈、巴勒斯坦）**，并注明「截止到上次更新时地球上只有这些国家和地区」；另保留「城市（选填）」输入框。
- **IP 显示**：个人主页显示最近登录 IP（注册 / 登录时自动记录）；获取不到时显示「未知」。
- **发布时间**：发布日记时自动记录发布时间，并在广场卡片、日记详情、我的日记、后台审核列表统一展示。
- **互动功能**：日记新增**点赞、转发、收藏**（广场 / 详情 / 我的日记 / 我的收藏均可操作）；转发会复制分享链接并计次；个人中心新增「我的收藏」列表。
- **注销账号**：普通用户与管理员均可注销本人账号（需输入密码二次确认，级联删除本人日记与互动数据）；出于安全考虑，**最后一个管理员不允许注销**。

### 🔒 安全加固（自查修补）

- **密码策略**：普通用户密码改为 **8-16 位、仅限字母和数字、须同时包含字母和数字**；管理员密码不受此限制；已创建账号密码不受影响。
- **密码存储**：所有密码均以「加盐 scrypt 散列」保存（`data/db.json` → `users[].passwordHash` + `users[].salt`），**绝不明文落盘**。
- **登录限流**：前台登录 / 注册、后台登录接口均加入 IP 频率限制（15 分钟内最多 10 次），防暴力破解与批量注册。
- **安全响应头**：新增 `X-Content-Type-Options`、`X-Frame-Options`、`Referrer-Policy` 与基础 CSP（限制脚本 / 图片 / 连接来源），并关闭 `X-Powered-By` 指纹。
- **输入校验**：地点经纬度范围校验、图片路径白名单（防路径穿越）、现有长度 / 格式校验保持。
- **统一错误文案**：登录失败统一返回「用户名或密码错误」，防止账号枚举。

### 🎨 界面改版

- 站点更名为 **栖桉集**，全站（前台 + 管理后台）主色调改为**淡蓝**。

### 📝 文档

- 更新 `README.md`（新功能、接口、部署说明）。
- 新增本更新日志 `CHANGELOG.md`。
- 版本号：`1.0.00`。
